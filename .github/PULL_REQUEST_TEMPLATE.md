## What changed

<!-- Describe the change and the motivation. -->

## Quality gates

- [ ] `check.bat` passes (`fmt --check` + `clippy -D warnings`)
- [ ] `build.bat` passes (release build + tests)
- [ ] Tests added/updated for the change
- [ ] `AGENTS.md` updated if a decision or workflow changed

## Token / latency impact

<!-- More compact payloads? Faster calls? "None" is a valid answer. -->
